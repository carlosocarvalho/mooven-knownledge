import { GitSearchUserEntity } from './git-search-user-entity';

describe('GitSearchUserEntity', () => {
  it('should create an instance', () => {
    expect(new GitSearchUserEntity()).toBeTruthy();
  });
});
