import { GitSearchResultEntity } from './git-search-result-entity';

describe('GitSearchResultEntity', () => {
  it('should create an instance', () => {
    expect(new GitSearchResultEntity()).toBeTruthy();
  });
});
